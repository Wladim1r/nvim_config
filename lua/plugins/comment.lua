return {
    "numToStr/Comment.nvim",
    opts = {
        -- Remap line-wise comment toggler (was gcc)
        toggler = {
            line = "cm",
        },
        -- Remap operator-pending comment (was gc)
        opleader = {
            line = "com",
        },
    },
    lazy = false,
}
